extends header

block content
  h1= title
  p Welcome to #{title}

    form(action='/foods/search' method='GET')
      legend Search Food Demo
        fieldset
          div
            label Food Name
            input(name='foodName')
          input(type='submit' onclick="foodData()" value='Delete')